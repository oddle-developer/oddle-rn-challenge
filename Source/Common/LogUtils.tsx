// import Global from 'lybrid-global'

function Logging() { };

Logging.writeLog = function (log: any) {
  // if(Global.sendLog) {
  //   return;    
  // }
}

Logging.getPath = function (callback: any) {
}

Logging.clearPath = function (callback: any) {
}

export default Logging;
